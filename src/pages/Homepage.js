import React from "react";
import PreviewCarousel from "../components/PreviewCarousel";

const Homepage = () => {
  return (
    <div>
      <h2>Homepage</h2>
      <PreviewCarousel text="Trending" />
    </div>
  );
};

export default Homepage;
